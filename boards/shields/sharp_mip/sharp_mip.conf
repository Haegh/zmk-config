# Enable the Sharp Memory-in-Pixel display
CONFIG_ZMK_DISPLAY=y

# LVGL v9 software rendering of the canvas widgets overflows the default
# 3072 byte display thread stack. Set (not default) so it beats ZMK's default.
CONFIG_ZMK_DISPLAY_DEDICATED_THREAD_STACK_SIZE=8192
